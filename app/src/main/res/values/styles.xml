<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="CustomTabStyle" parent="Widget.Design.TabLayout">
        <!-- Decrease padding (top, bottom, left, right) -->
        <item name="tabPaddingStart">1dp</item> <!-- Left padding -->
        <item name="tabPaddingTop">1dp</item> <!-- Top padding -->
        <item name="tabPaddingEnd">1dp</item> <!-- Right padding -->
        <item name="tabPaddingBottom">1dp</item> <!-- Bottom padding -->
        <item name="tabTextAppearance">@style/CustomTabTextAppearance</item>
    </style>

    <style name="CustomTabStyle2" parent="Widget.Design.TabLayout">
        <!-- Decrease padding (top, bottom, left, right) -->
        <item name="tabPaddingStart">1dp</item> <!-- Left padding -->
        <item name="tabPaddingTop">1dp</item> <!-- Top padding -->
        <item name="tabPaddingEnd">1dp</item> <!-- Right padding -->
        <item name="tabPaddingBottom">1dp</item> <!-- Bottom padding -->
        <item name="tabTextAppearance">@style/CustomTabTextAppearance2</item>
    </style>


    <style name="CustomTabTextAppearance">
        <item name="android:textSize">11sp</item> <!-- Adjust the text size here -->
        <item name="android:textStyle">bold</item>
        <item name="android:gravity">left</item>
        <item name="android:textColor">@android:color/black</item> <!-- Optional: Change text color -->
    </style>

    <style name="CustomTabTextAppearance2">
        <item name="android:textSize">11sp</item> <!-- Adjust the text size here -->
        <item name="android:gravity">left</item>
        <item name="android:textColor">@android:color/black</item> <!-- Optional: Change text color -->
    </style>

    <style name="CustomTextInputStyle">
        <item name="hintTextColor">@color/white</item>
        <item name="iconTint">@color/white</item>
        <item name="android:textColorHint">@color/white</item>
    </style>

    <style name="CustomDialogTheme" parent="Theme.MaterialComponents.Light.Dialog">
        <item name="android:background">@color/white</item>
    </style>


    <style name="CustomSearchView" parent="Widget.AppCompat.SearchView">
        <item name="queryHint">Search here...</item>
        <item name="android:textColorHint">@color/black</item>
        <item name="android:textColor">@color/black</item>
    </style>


>>>>>>> f91b974 (ui update)
</resources>